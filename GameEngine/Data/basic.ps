#version 330

in vec2 texCoords;

out vec4 finalColor;

uniform vec3 diffuseColor;
uniform sampler2D diffuseTexture;

void main()
{
    //finalColor = vec4( texCoords.x, 0.5, texCoords.y, 1 );
    finalColor = vec4( diffuseColor * texture( diffuseTexture, texCoords ).rgb, 1 );
}